# DO NOT RUN THIS FILE
P0="/org/gnome/settings-daemon/plugins/media-keys/custom-keybindings/custom0/"
gsettings set $BIND_PATH:$P0 name "unkwon"
gsettings set $BIND_PATH:$P0 command "unkwon"
gsettings set $BIND_PATH:$P0 binding "unkwon"
# This will help with KeyBinds
# P0 you chagen the name every time to like OO, HI Needs to be CAPS 
# /org/gnome/settings-daemon/plugins/media-keys/custom-keybindings/custom0/ custom0 you have to chagen it every time custom1, custom2, etc
# gsettings set $BIND_PATH:$P0 name "unkwon" name will be the name of the keybind
# gsettings set $BIND_PATH:$P0 command "unkwon" command it the command
# gsettings set $BIND_PATH:$P0 binding "unkwon" binding is keys
# <alt> = alt, <Shift> = shift, <Control> = ctrl, <Super> = windows key
# for letter it is lower case only